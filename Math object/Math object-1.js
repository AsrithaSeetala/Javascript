var number = 5.7;
 
// var newNumber = 30 + Math.ceil(number);

var newNumber = 30 + Math.round(number);

// var newNumber = 30 + Math.floor(number);

document.write(newNumber);

// Math object in Javascript
